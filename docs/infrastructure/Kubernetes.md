# Kubernetes Infrastructure
## Overview
The cluster should have auto-scaling enabled. Nodes are divided into CPU-only pools for generic tasks and GPU pools for training/serving.
## High Availability
Deploy control plane components across multiple availability zones.
